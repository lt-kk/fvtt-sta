<div class="dice-roll">
  {{#if flavor}}
    <div class="dice-flavor">{{flavor}}</div>
  {{/if}}
  <div class="dice-result">
    <div class="dice-formula">
      {{localize (concat "sta.roll.challenge._singular")}}:
      {{formula}}
    </div>
    <div class="dice-tooltip">
      <section class="tooltip-part">
        <div class="dice">
          <ol class="dice-rolls">
            {{#each tooltip.rolls}}
              <li class="roll {{classes}}">{{{value}}}</li>
            {{/each}}
          </ol>
        </div>
      </section>
      <div class="actions">
        <button class="test">Do something nice</button>
      </div>
    </div>
    <h4 class="dice-total">
      <span class="successes">
        {{localize "sta.roll.success._plural"}}:
        {{staData.result.successes}}
      </span>
      {{#if staData.result.complications}}
        <span class="complications">
          {{localize "sta.roll.complication._plural"}}:
          {{staData.result.complications}}
        </span>
      {{/if}}
    </h4>
  </div>
</div>
